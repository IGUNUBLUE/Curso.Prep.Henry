// arrays: Imaginemos un array como los hicimos con las variables, supongamos que es un cuarto en el cual se pueden guardar algo.
// pero en esta caso en ese lugar se pueden almacenar varios tipos de objetos e indentificarlos con algo parecido a una dirreción
// la cual utilizaremos para encontrarlos cuando los necesitemos. Por ejemplo mi primer objeto (dato), como fue el primero lo pondremos
// en el espacio 1 (0).